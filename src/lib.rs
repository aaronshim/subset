#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
    }

    // We need to assume directory trasversal and md5 digester are working as advertised (they can be mocked away)

    // Test the logic for generating lists of missing files
    
    // Test the logic for generating maps

    // Test the logic for generating lists of missing files in both directions
    
    // Test the logic for printing logic?

    // Integration tests with examples?
}
